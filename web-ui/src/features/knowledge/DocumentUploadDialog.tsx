import { useState } from "react";
import { Upload, X } from "lucide-react";
import { useAuth } from "@/features/auth/AuthProvider";
import { apiRequest } from "@/lib/api-client";
export function DocumentUploadDialog({ open, onClose }: { open: boolean; onClose: () => void }) { const { auth } = useAuth(); const [file, setFile] = useState<File | null>(null); const [title, setTitle] = useState(""); const [description, setDescription] = useState(""); const [type, setType] = useState("qa"); const [roles, setRoles] = useState<string[]>([]); if (!open) return null; const choose = (next: File) => { setFile(next); const name = next.name.replace(/\.[^.]+$/, ""); setTitle(name); setDescription(name); }; const submit = async () => { if (!file) return; const form = new FormData(); form.append("file", file); form.append("title", title); form.append("description", description); form.append("knowledge_base_type", type); roles.forEach((role) => form.append("accessible_by", role)); await apiRequest("/v1/api/document/upload", { method: "POST", body: form, token: auth?.token }); onClose(); }; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"><div className="w-full max-w-lg rounded-lg bg-background p-5 shadow-lg"><div className="mb-4 flex items-center"><h2 className="text-lg font-semibold">上传文档</h2><button type="button" aria-label="关闭" className="ml-auto" onClick={onClose}><X className="h-4 w-4" /></button></div><div data-testid="upload-dropzone" onDragOver={(e) => e.preventDefault()} onDrop={(e) => { e.preventDefault(); if (e.dataTransfer.files[0]) choose(e.dataTransfer.files[0]); }} className="mb-4 rounded-md border-2 border-dashed p-6 text-center"><Upload className="mx-auto h-6 w-6" /><p className="mt-2 text-sm">拖拽文件到此处</p><input type="file" aria-label="选择文件" onChange={(e) => { if (e.target.files?.[0]) choose(e.target.files[0]); }} /></div><label className="block text-sm">标题<input aria-label="标题" value={title} onChange={(e) => setTitle(e.target.value)} className="mt-1 w-full rounded border p-2" /></label><label className="mt-3 block text-sm">描述<textarea aria-label="描述" value={description} onChange={(e) => setDescription(e.target.value)} className="mt-1 w-full rounded border p-2" /></label><label className="mt-3 block text-sm">知识库类型<select aria-label="知识库类型" value={type} onChange={(e) => setType(e.target.value)} className="mt-1 w-full rounded border p-2"><option value="qa">问答库</option><option value="data">数据查询</option></select></label><label className="mt-3 block text-sm">角色（多选）<select multiple aria-label="角色" value={roles} onChange={(e) => setRoles(Array.from(e.target.selectedOptions, (o) => o.value))} className="mt-1 w-full rounded border p-2"><option value="admin">管理员</option><option value="editor">编辑者</option></select></label><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded border px-3 py-2">取消</button><button type="button" disabled={!file} onClick={() => void submit()} className="rounded bg-primary px-3 py-2 text-primary-foreground">上传</button></div></div></div>; }
